#ifndef SHARED_H
#define SHARED_H

#include "raylib.h"

// Shared enum between all game files
typedef enum GameScreen {
    MENU,
    QUIZ,
    DODGE,
    COLLECT,
    VICTORY
} GameScreen;

#endif
